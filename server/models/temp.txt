Viewing Previous Orders
To fetch a user along with their order details, including the items ordered and their quantities:

javascript
Copy code
const user = await User.findById(userId).populate({
  path: 'orders',
  populate: {
    path: 'items.item',
    model: 'Item',
  },
});
res.json(user);